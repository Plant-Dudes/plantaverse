import{_ as e,f as t}from"./app.2bf926e2.js";const o={},a=t('<p>The theme supports blog feature with<code>vuepress-plugin-blog2</code> by default, and it&#39;s <strong>disabled by default</strong>.</p><p>If you need blog functionality, you can set <code>themeConfig.plugins.blog</code> to <code>true</code> to enable blog functionality.</p><h2 id="intro" tabindex="-1"><a class="header-anchor" href="#intro" aria-hidden="true">#</a> Intro</h2><p>After enabling the blog function, the theme allows you to configure categories, tags, whether it is an article, whether it appears in the timeline, stars, sticky and other functions through the frontmatter of the page.</p><h2 id="i18n-support" tabindex="-1"><a class="header-anchor" href="#i18n-support" aria-hidden="true">#</a> I18n Support</h2><p>The theme adds support for i18n in blog system. You can set differernt blog config for each language in <code>themeConfig.locales</code>.</p><p>When you have multiple languages, the article list, timeline, etc. under each language will remain independent.</p><h2 id="sidebar" tabindex="-1"><a class="header-anchor" href="#sidebar" aria-hidden="true">#</a> Sidebar</h2><p>The theme provides a blog info sidebar. The sidebar will be displayed on the blog-related page (it will be displayed on the right side on the desktop, and retracted into the sidebar in mobile view)</p><p>You can control the display behavior of the sidebar on non-blog related pages through <code>blog.sidebarDisplay</code> in themeConfig. Optional values are <code>&quot;mobile&quot; | &quot;none&quot; | &quot;always&quot;</code>. The default is <code>&quot;mobile&quot;</code>, that is, when you visit non-blog related pages in mobile view, you can also see it in the sidebar.</p><h2 id="pagination" tabindex="-1"><a class="header-anchor" href="#pagination" aria-hidden="true">#</a> Pagination</h2><p>For the list of articles on all pages, we will display a pagination component at the bottom. You can use this component to quickly jump to the first page, the last page, and the two pages before and after. You can also enter a number to jump to the specified page.</p><p>The default number of articles per page is <code>10</code>, you can set <code>blog.articlePerPage</code> in themeConfig to override it.</p>',13);function i(n,r){return a}var s=e(o,[["render",i]]);export{s as default};
